import { connect } from 'react-redux';

import TripDetailScreen from './view';

const mapStateToProps = () => {
  return {};
};

const mapDispatchToProps = () => ({});

export default connect(mapStateToProps, mapDispatchToProps)(TripDetailScreen);
